@{
    var loggedIn = User.Identity.IsAuthenticated;
}

@if (loggedIn)
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Identity" asp-action="Signout">Sign out</a>
    </li>
    <li class="nav-item">
        @* This how we can acces the claims details in the Identity in the UI *@
        <label>Hello @User.Identity.Name (@User.Claims.FirstOrDefault(c => c.Type == "Department"))</label>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Identity" asp-action="Signup">Sign up</a>
    </li>

    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Identity" asp-action="Signin">Sign in</a>
    </li>
}